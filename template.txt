package main
{{range .functions}}
    {{.}}
{{end}}
func main() {
	{{- range .commands}}
        {{if not .Hidden}}
            {{- .Src -}}
        {{end}}
    {{end}}
}

// used to avoid "declared and not used" error
func use(vals ...interface{}) {
    for _, val := range vals {
        _ = val
    }
}
